import { moduleFor, test } from 'ember-qunit';

moduleFor('view:dashboard/courses/show', 'Unit | View | dashboard/courses/show');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
