import { moduleFor, test } from 'ember-qunit';

moduleFor('view:dashboard/courses/edit', 'Unit | View | dashboard/courses/edit');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
