import { moduleFor, test } from 'ember-qunit';

moduleFor('view:dashboard/profile/edit', 'Unit | View | dashboard/profile/edit');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
