import { moduleFor, test } from 'ember-qunit';

moduleFor('view:dashboard/grades/new', 'Unit | View | dashboard/grades/new');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
