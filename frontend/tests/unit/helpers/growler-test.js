import { growler } from '../../../helpers/growler';
import { module, test } from 'qunit';

module('Unit | Helper | growler');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = growler(42);
  assert.ok(result);
});
